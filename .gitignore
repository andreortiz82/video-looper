# media (do not commit) — includes nested audio/<session>/ downloads
audio/**
!audio/.gitkeep
output/**
!output/.gitkeep
stills/*.png
video/

# live queue (status + Drive ids). Copy queue.example.json → queue.json
queue.json
instagram-queue.json
queue.json.tmp

# env
.venv/
.env
.DS_Store
*.pyc
__pycache__/
